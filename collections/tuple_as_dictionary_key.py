priority_index = {
    (1, 'premier'): [1, 34, 12],    #multiple item in the key
    (1, 'mvp'): [84, 22, 12],
    (2, 'standard'): [93, 81, 3],
}

print(list(priority_index.keys()))

# print(list(priority_index.keys()[1])) #doesn't work